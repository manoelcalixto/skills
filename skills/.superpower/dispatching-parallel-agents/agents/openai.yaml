interface:
  display_name: "Dispatching Parallel Agents"
  short_description: "Parallelize independent investigations and fixes"
  default_prompt: "Use $dispatching-parallel-agents to split independent failures into parallel sub-agent tasks."

